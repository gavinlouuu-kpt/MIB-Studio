#pragma once

#include <string>

namespace MenuSystem
{

    void clearInputBuffer();
    void displayMenu();
    void runMockSample();
    void runLiveSample();
    void convertSavedImages();
    int runMenu();

} // namespace MenuSystem